export class Behaviour{
    id: number;
    name: string;
    activity: boolean;

    constructor(id: number, name: string, activity: boolean){
        this.id = id;
        this.name = name;
        this.activity = activity;
    }
}